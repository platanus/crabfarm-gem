class <%= surveyor_class %> < Crabfarm::BaseSurveyor

  def run
    # You can replace the following line after running the owner navigator specs once.
    # Take a look at the 'Testing' section of the README.md for more information!
    take_snapshot
  end

end

